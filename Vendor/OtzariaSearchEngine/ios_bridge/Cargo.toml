[package]
name = "otzaria_search_engine_ios"
version = "0.1.0"
edition = "2021"

[lib]
name = "otzaria_search_engine_ios"
crate-type = ["staticlib"]

[dependencies]
search_engine = { path = "../upstream/otzaria_search_engine/rust" }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
libc = "0.2"
